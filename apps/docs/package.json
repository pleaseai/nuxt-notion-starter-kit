{
  "name": "@pleaseai/nuxt-notion-docs",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "nuxt dev --extends @pleaseai/notion-nuxt-layer",
    "build": "nuxt build --extends @pleaseai/notion-nuxt-layer",
    "generate": "nuxt generate --extends @pleaseai/notion-nuxt-layer",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "type-check": "nuxt typecheck",
    "clean": "rm -rf .nuxt .output dist",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix"
  },
  "dependencies": {
    "@nuxt/eslint": "catalog:",
    "@pleaseai/notion-nuxt-layer": "workspace:*",
    "nuxt": "catalog:",
    "vue": "catalog:",
    "vue-router": "catalog:"
  },
  "devDependencies": {
    "typescript": "catalog:",
    "eslint": "catalog:"
  }
}
